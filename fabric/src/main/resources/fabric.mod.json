{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "Cristelknight"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}"
  },
  "license": "${mod_license}",
  "icon": "pack.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "de.cristelknight.playernames.fabric.PlayerNamesFabric"
    ]
  },
  "mixins": [
    "svc_player_names.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_min_version}",
    "minecraft": ">=${minecraft_version}",
    "voicechat": ">=${minecraft_version}-${voicechat_version}",
    "cristellib": ">=${cristellib_version}",
    "java": ">=21"
  }
}
